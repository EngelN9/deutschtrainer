請將以下內容視為 DeutschTrainer 現有規格的「補充產品策略與技術執行規格」。

請先完整檢查目前 repository 的實際程式碼、文件、環境變數範例、資料庫 migration、測試、 CI、Expo/EAS 設定與部署文件，再進行任何修改。

不要重新撰寫、刪除或重做原有功能規格；既有功能、資料模型、API contract、學習邏輯、安 全機制與已完成階段都應盡量保留。本次工作的主要目的是確立 App、網站、管理後台與後端 的產品邊界，並使專案能從本機開發狀態逐步進入可實機驗收、可部署、可公開使用的狀態。

# # 一、產品方向

# DeutschTrainer 採用以下產品策略：

# 1. Android App 是現階段主要的學習者產品。

2. 管理員後台維持為網站，供課程、題目、版本、審核、AI 草稿及發布管理使用。

3. 建立簡潔的公開網站，作為產品介紹、下載入口、隱私權政策、使用條款、聯絡方式與帳號 刪除說明頁面。

4. 學習者網頁版保留，但不是目前第一優先。

5. 不建立兩套互不相容的 App 與網站系統。

6. App、學習者網頁版、管理員後台必須共用同一套後端、Supabase、帳號、課程資料、學習 紀錄、評分系統與核心 domain logic。

# 目標架構如下：

- apps/mobile：

- Expo + React Native。

- Android 為第一優先。

- iOS 保留支援，但暫時不是發布優先項目。

- Web 平台保留，未來作為學習者網頁版基礎。

- apps/admin：

- Next.js。

- 作為管理員後台。

- 管理功能必須受角色與權限保護。

- apps/api：

- 所有需要伺服器權限、AI 金鑰、service-role、受保護資料寫入、音訊處理與正式評分的功能 都由 API 負責。

- Supabase：

- 統一處理資料庫、Authentication、Storage、RLS 與必要的基礎設施。

- packages：

- 共享型別、驗證、評分、學習引擎、AI schema 與資料存取 contract。

- 不要在各 frontend 重複實作相同商業邏輯。

# # 二、開發優先順序

# 請依下列優先順序規劃，不要同時大規模擴充所有平台。

## Priority 1：Android 實機版本

# 先使 Android App 能夠在非開發者電腦環境下正常執行，至少完成：

- App 可連線到遠端 HTTPS API。

- 可正常註冊、登入、登出及恢復 session。

- 可完成 onboarding。

- 可讀取正式發布的課程內容。

- 可提交固定題型與 AI 題型。

- 可同步學習紀錄。

- 可進行寫作、聽力與口說功能。

- 可測試離線課程、離線作答及重新連線同步。

- 可正確處理 API、Supabase、網路中斷、驗證失效與伺服器錯誤。

- 可建立 EAS preview build，供 Android 實機驗收。

- 後續可建立 production build。

# ## Priority 2：管理員網站部署

# 確認 apps/admin 可部署至正式或 staging 環境，並且：

- 未登入者不可存取管理功能。

- 非管理員不可存取管理功能。

- 管理頁面的 API 與 Supabase public configuration 使用環境變數。

- service-role key 與 OpenAI key 不得出現在瀏覽器 bundle。

- 管理員可依原有規格管理、審核及發布內容。

- 正式 build、typecheck 與權限檢查必須通過。

# ## Priority 3：公開網站

# 建立最小但完整的公開頁面，包括：

- 首頁／產品介紹。

- 支援程度與主要功能介紹。

- Android App 下載或測試入口。

- 隱私權政策。

- 使用條款。

- 帳號刪除說明。

- 聯絡方式或支援方式。

# 請先評估最低維護成本的實作方式。

優先考慮在現有 Next.js 專案中建立公開 route，並將管理區集中於受保護的 `/admin` 路徑； 只有在程式架構、安全隔離或部署需求明確不適合時，才新增獨立的 `apps/site`。

不應為了簡單的公開頁面建立不必要的第二套 API、資料庫或 authentication。

# ## Priority 4：學習者網頁版

學習者網頁版使用現有 Expo Web 能力逐步開放，不要另外重寫一套完整 frontend。

# 第一階段網頁版可支援：

- 註冊與登入。

- 課程閱讀。

- 單字與文法查詢。

- 一般固定題型。

- 寫作練習。

- 學習進度查看。

# 原生能力較強的功能仍以 App 為主要平台，例如：

- 完整離線下載。

- 本機通知。

- 麥克風錄音。

- 音訊檔案處理。

- 裝置權限。

- 背景或裝置層級行為。

不要為了讓 Web 與 Android 立即達到百分之百功能一致，而破壞 Android 體驗或增加大量 platform-specific workaround。

# # 三、目前 Android 連線錯誤的處理原則

目前 Android 實機曾出現：

java.net.UnknownServiceException: CLEARTEXT communication to 127.0.0.1 not permitted by network security policy

請將此問題納入環境設定修正，但不要把開發期 workaround 當成正式架構。

# 需要明確區分：

# ## 本機開發環境

- Android Emulator 可依需要使用 10.0.2.2。

- USB 實機開發可使用 adb reverse。

- 開發環境可在必要時允許 HTTP cleartext。

- 本機 API 可能使用 localhost 或區域網路 IP。

- 相關設定只能限於 development 或 debug build。

## Staging／Production

- 不得使用 127.0.0.1。

- 不得使用 localhost。

- 不得使用 10.0.2.2。

- 不得依賴 adb reverse。

- 不得依賴開發者電腦。

- API base URL 必須是可由實機公開存取的 HTTPS URL。

- Supabase URL 必須指向遠端 project。

- 不應全域允許 cleartext HTTP。

- production build 不得包含本機測試 URL。

請檢查 App 目前所有 API URL、Supabase URL、audio URL、deep link、callback URL 與其 他網路位置，確認沒有硬編碼的 localhost、127.0.0.1、10.0.2.2 或本機 IP。

# # 四、環境變數與設定策略

# 建立清楚且可驗證的環境分層：

- local

- preview 或 staging

- production

# 至少應涵蓋：

- API base URL。

- Supabase URL。

- Supabase anon key。

- App environment。

- AI fake mode。

- feature flags。

- logging 或 telemetry level。

- App version／release identifier。

- Web origin。

- CORS allowlist。

- deep link／redirect URL。

# 請遵守：

1. 所有 `EXPO_PUBLIC_*` 與 `NEXT_PUBLIC_*` 都視為公開資訊。

2. service-role key 不得放入 Mobile、Web 或公開環境變數。

3. OpenAI API key 不得放入 Mobile、Web 或公開環境變數。

4. production 與 staging 必須拒絕 placeholder secret。

5. production 與 staging 必須拒絕 fake AI mode。

6. production 與 staging 必須拒絕不安全的本機 API URL。

7. 缺少必要環境變數時，應在啟動或 build 階段明確失敗，不要靜默 fallback 到 localhost。

8. 不要將真實 secret 寫入 repository。

9. 更新 `.env.example` 與相關文件，但只提供 placeholder。

10. 對 Mobile、Admin 與 API 分別定義可公開與不可公開的設定邊界。

請避免散落在多個檔案中的 `process.env`、`Constants.expoConfig.extra` 或硬編碼 fallback。 應集中建立 typed configuration module，並使用 Zod 或現有 validation 工具驗證。

# # 五、共用架構要求

請維持 monorepo 的單一來源原則。

# ## 必須共用

- Domain types。

- Request／response schema。

- 課程與 exercise schema。

- deterministic grading。

- mastery calculation。

- spaced review scheduling。

- API error contract。

- pagination contract。

- release identifier。

- content version。

- authentication state相關型別。

- 可跨平台共用的 UI token 或基礎元件。

# ## 不應共用

# 不要強行共用高度依賴平台的實作，例如：

- Android notification implementation。

- Web notification implementation。

- Android file-system implementation。

- Browser storage implementation。

- 原生 audio recording。

- 瀏覽器 MediaRecorder。

- 原生 navigation container。

- Next.js server component。

應使用 interface、adapter 或 platform-specific file，而不是在單一檔案中堆疊大量條件判斷。

# # 六、Android App 的設計原則

# Android App 是主要學習者介面，請優先保證：

- 手機直向操作體驗。

- 輸入欄位不被鍵盤遮住。

- 可安全處理瀏海、狀態列與 navigation bar。

- loading、empty、offline、error、expired session 狀態完整。

- 不直接向使用者顯示原始 Java exception、stack trace 或內部 URL。

- 所有錯誤訊息應轉成可理解的繁體中文。

- 技術細節只寫入 development log。

- 表單必須防止重複提交。

- API mutation 應支援 idempotency 或沿用既有 idempotency 機制。

- 登入狀態恢復期間避免 navigation 閃爍。

- 需要權限時才要求權限。

- 麥克風權限被拒絕時提供替代流程。

- 沒有網路時清楚說明哪些功能仍可使用。

- 離線佇列不可造成重複作答或進度倒退。

- App 重新啟動後，必要的下載、pending attempt 與同步狀態應可恢復。

# # 七、網站的產品邊界

# ## 管理員網站

管理員網站以桌面操作為優先，不必追求完全等同手機介面。

# 應強化：

- 表格與大量資料管理。

- 搜尋、篩選、排序及分頁。

- 草稿、審核、發布狀態。

- 未儲存變更提醒。

- 權限不足頁面。

- 破壞性操作確認。

- audit trail。

- server-side authorization。

- 不只依賴前端隱藏按鈕。

# ## 公開網站

公開網站應保持簡單、快速、可索引、可部署。

# 要求：

- 不依賴登入。

- 不載入管理員專用程式碼。

- 基本 SEO metadata。

- favicon、Open Graph metadata 與產品名稱。

- 支援繁體中文。

- 預留未來多語系可能性，但不要現在過度設計。

- 隱私權政策與帳號刪除頁面應有永久且可公開存取的 URL。

- 不要在公開頁暴露管理 API、內部 health URL 或敏感環境資訊。

# # 八、部署策略

# 請讓專案形成下列 staging 架構：

- Remote Supabase staging project。

- Deployed HTTPS API。

- Deployed Admin／public website。

- EAS Android preview build。

- Mobile、Admin 與 API 使用同一 staging environment。

- staging 資料與 production 資料完全分離。

# API 部署必須：

- 使用既有 production bundle／container。

- 綁定 `0.0.0.0`。

- 使用平台提供的 `PORT`。

- 提供 health endpoint。

- 正確處理 SIGINT／SIGTERM。

- 不以 root user 執行 container。

- secrets 由部署平台注入。

- 設定正確 CORS allowlist。

- 不允許任意 origin 攜帶憑證。

- 不在 health response 洩露 secret。

- logging 不得記錄 access token、refresh token、密碼、service-role key、OpenAI key 或完整 私人內容。

# 請避免建立只能在開發者電腦上執行的部署流程。

# # 九、安全要求

# 請維持並檢查：

- Supabase RLS。

- server-side ownership check。

- admin role check。

- learner data isolation。

- writing／audio workspace isolation。

- signed URL 或受保護音訊存取。

- answer key 不得由 client 任意取得。

- fixed grading 的 server-authoritative 流程。

- AI evaluation 的 server-only key。

- rate limiting。

- idempotency。

- request validation。

- response validation。

- delete account／delete data 流程。

- audit trail。

- 錯誤回應不得暴露 SQL、stack trace、secret 或內部檔案路徑。

不要只以 TypeScript 型別作為安全驗證；所有外部輸入必須有 runtime validation。

# # 十、版本與發布管理

# 請建立一致的版本管理方式：

- Expo app version。

- Android versionCode。

- iOS buildNumber。

- API release identifier。

- course content version。

- database migration version。

- build commit SHA。

至少應使 App 的診斷或設定頁在不洩露敏感資訊的前提下顯示：

- App version。

- build number。

- environment。

- API release identifier。

- content release identifier。

# 正式發布前，確認版本號不會因重建而倒退。

# # 十一、測試要求

不要只以「build 成功」判定完成。

# 請保留既有測試，並依修改範圍補充測試。

# 至少驗證：

# ## Configuration

- local 可接受本機 HTTP。

- staging／production 拒絕 localhost。

- staging／production 拒絕 HTTP API URL。

- staging／production 拒絕 fake AI mode。

- 缺少必要設定會明確失敗。

- client bundle 中不存在 server secret。

# ## Authentication

- 註冊。

- 登入。

- 登出。

- session restore。

- expired token。

- 未登入導向。

- 非 admin 無法進入管理區。

# ## Android 實機流程

- App 首次啟動。

- onboarding。

- 課程載入。

- 固定題型送出。

- AI 題型送出。

- 寫作。

- audio playback。

- microphone permission granted／denied。

- 離線作答。

- reconnect sync。

- 重複提交保護。

- 錯誤畫面不顯示 raw exception。

# ## Website

- 公開頁面不需登入。

- `/admin` 需要登入及 admin 權限。

- public build 不包含 admin secret。

- privacy、terms、account deletion URL 可公開存取。

# ## CI

# CI 至少應維持或通過：

- format check。

- lint。

- strict typecheck。

- unit／integration tests。

- API production bundle。

- API health smoke test。

- container build。

- Expo Doctor。

- Android export。

- Web export。

- Admin production build。

# # 十二、實作方式

# 請採取小步驟、可審查、可回復的修改方式。

# 執行前：

1. 讀取 repository 結構與現有文件。

2. 找出目前 API base URL 的來源。

3. 找出所有硬編碼 localhost／127.0.0.1／10.0.2.2。

4. 找出 Mobile、Admin、API 的環境變數載入方式。

5. 檢查 Expo、EAS、Supabase、Docker 與 CI 設定。

6. 檢查公開網站目前是否已存在。

7. 檢查 admin route 的實際權限保護方式。

8. 檢查 Android cleartext 設定是否會進入 production。

# 實作時：

- 優先修正根本原因，不要只隱藏錯誤訊息。

- 不要進行與目標無關的大規模 refactor。

- 不要更換整套 framework。

- 不要把 Expo App 重寫成原生 Kotlin。

- 不要把 Next.js 重寫成其他 frontend framework。

- 不要新增第二套 database。

- 不要新增第二套 authentication。

- 不要複製現有 domain logic。

- 不要降低現有 RLS 或 API authorization。

- 不要提交真實憑證。

- 不要刪除既有 migration。

- 不要修改已套用 migration 的歷史內容；需要變更時新增 migration。 - 不要假設文件內容一定與程式一致，必須以實際程式碼驗證。

# # 十三、Codex 的回報格式

每次工作請先輸出一份簡短但具體的檢查結果，再進行修改。

# 回報至少包含：

1. Current State

- 目前實際架構。

- 已存在的功能。

- 與本補充規格相符之處。

- 不相符或尚未完成之處。

2. Proposed Changes

- 預計修改的檔案。

- 每個修改的目的。

- 是否涉及 migration。

- 是否涉及環境變數。

- 是否涉及部署平台。

# 3. Risk Assessment

- authentication 風險。

- RLS／authorization 風險。

- offline sync 風險。

- Android／Web 相容性風險。

- deployment 風險。

4. Implementation

- 以小批次完成。

- 每批修改後執行相關測試。

- 測試失敗時先修復，不要略過。

# 5. Final Verification

- 列出實際執行的 command。

- 列出通過與失敗的測試。

- 列出仍需人工完成的外部步驟。

# - 不得宣稱未實際驗證的功能已完成。

# # 十四、建議的執行階段

# 請將後續工作拆成以下階段，不要一次進行超大修改。

# ## Stage A：環境與連線整理

- 統一 typed environment configuration。

- 移除 production 本機 URL fallback。

- 建立 local／staging／production 驗證。

- 修正 Android 本機實機連線流程。

- 確認 cleartext 僅限 development。

- 更新環境設定文件。

# ## Stage B：遠端 staging

- 準備遠端 Supabase staging。

- 部署 API container。

- 設定 HTTPS API URL。

- 設定 CORS。

- 設定 Mobile 與 Admin staging variables。

- 驗證 health、auth、course、grading、writing、audio 與 sync。

涉及真實外部憑證、付費平台或無法由 repository 自動完成的步驟時，不要虛構結果。請建立 清楚的人工操作 checklist。

# ## Stage C：Android Preview

- 設定 EAS preview profile。

- 驗證 public environment variables。

- 建立 Android preview build。

- 提供實機 smoke-test checklist。

- 確認 App 不依賴 USB、adb reverse 或開發者電腦。

# ## Stage D：公開網站與 Admin

- 確認公開 route 的最佳位置。

- 建立公開首頁。

- 建立 privacy、terms、account deletion 頁面。

- 保護 `/admin`。

- 完成 production build 與 deployment checklist。

## Stage E：學習者 Web

- 評估現有 Expo Web 相容性。

- 先開放基本課程與練習。

- 針對原生功能提供清楚的 capability fallback。

- 不阻塞 Android 正式版本。

# # 十五、完成定義

本補充規格不能只以「程式碼已加入」作為完成。

Android 第一階段完成的最低標準是：

- 安裝到未連接開發者電腦的 Android 手機。

- 使用 staging build。

- 透過 HTTPS 連線遠端 API。

- 可完成註冊、登入、課程、作答與進度同步。

- 不出現 127.0.0.1、localhost 或 cleartext 錯誤。

- 不顯示原始 exception 給一般使用者。

- 不包含任何 server secret。

- CI 與必要測試通過。

- 尚未驗證的功能明確標示為待驗收。

公開網站第一階段完成的最低標準是：

- 可公開開啟。

- 不需登入即可閱讀產品資訊、隱私權政策、使用條款與帳號刪除說明。

- 管理員路徑受身份與角色保護。

- 網站與 App 使用相同品牌、產品名稱及正式支援資訊。

請先執行 repository audit，依上述格式回報 Current State、差距與 Stage A 的實作計畫。未完 成 audit 前，不要進行大規模重構。
