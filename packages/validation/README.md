# @deutschtrainer/validation

Zod schemas for public API requests, responses, and shared error formats.

Every API operation should have separate request and response schemas. Database row types should not be used as API contracts.
