# @deutschtrainer/shared-types

Domain-level TypeScript models shared by mobile, admin, API, validation, grading, and learning-engine packages.

Database row types, API DTOs, and UI view models must remain separate from these domain models.
